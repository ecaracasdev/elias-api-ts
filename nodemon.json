{
  "watch": ["src"],
  "ext": "ts, json, js",
  "ignore": ["src/**/*.spec.ts", "node_modules", "coverage", "build"],
  "exec": "ts-node -r tsconfig-paths/register ./src/index.ts",
  "restartable": "rs",
  "env": {
    "NODE_ENV": "development"
  }
}
